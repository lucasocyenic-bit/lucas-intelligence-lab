# three/

React Three Fiber scenes and primitives (Knowledge Universe graph, ambient
backgrounds, particle fields). Always dynamically imported with `next/dynamic`
and `ssr: false` — never load on initial paint. Built starting Phase 8.
