# config/

Static site configuration: nav items, site metadata, project registry (the
five flagship experiences), feature flags. Single source of truth so nav,
homepage, and SEO metadata never drift out of sync.
